# OpenPlan AI 서비스 — 최소 의존성 (W2 뼈대)
fastapi>=0.115
uvicorn[standard]>=0.30
# 상용(Gemini·Claude·GPT)·로컬(Ollama/Qwen) 통합 게이트웨이 = "콘센트"
# 상한 고정 이유: 1.92+ 는 Python 3.11 용 wheel 이 없어 sdist 빌드로 떨어지고, 그때 Rust/Cargo 를
# 요구해 설치가 깨진다(2026-07-25 실측). Python 3.12+ 로 올린 뒤에 상한을 풀 것.
litellm>=1.50,<1.92
pydantic>=2.7
pydantic-settings>=2.3
# Windows 는 OS에 IANA tz DB가 없어 zoneinfo("Asia/Seoul") 이 ZoneInfoNotFoundError 로 죽는다
# (2026-08-02 실측, /plans/draft 스키마의 zone 검증에서 재현). Linux/WSL 은 OS DB가 있어 없어도
# 되지만, 두 플랫폼에서 동일하게 동작해야 하므로 명시적으로 고정한다.
tzdata

# ── 테스트 (W3) ─────────────────────────────────────────────
# 모델 호출은 전부 monkeypatch로 목 처리 — API 키 없이 전체 스위트가 통과해야 한다.
pytest>=8.0
httpx>=0.27          # fastapi.testclient.TestClient 가 요구
